"""Utility sub-package for diskripr.

Contains shared helpers that do not belong to a specific pipeline stage:

- ``progress``    — Progress event protocol and callback type definition.
- ``filesystem``  — Path manipulation and Jellyfin directory structure helpers.
"""
